2026-04-13
- Initialized project tracking files and created the first `pyproject.toml` for `liteseries` so the package has build metadata and declared runtime dependencies. Commit tag: `init-import-1`.
- Added stage-1 import scaffolding with `tests/unit`, `tests/integration`, `tests/report`, plus an integration smoke test for the public API. Commit tag: `init-import-1`.
